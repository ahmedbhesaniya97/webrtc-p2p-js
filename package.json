{
  "name": "webrtc-mqtt-broker",
  "version": "1.0.0",
  "description": "MQTT broker server for WebRTC signaling",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-certs": "mkdir -p certs && openssl req -nodes -new -x509 -keyout certs/server.key -out certs/server.cert -subj '/CN=localhost'"
  },
  "keywords": [
    "webrtc",
    "mqtt",
    "signaling",
    "broker",
    "video",
    "audio",
    "call"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aedes": "^0.48.1",
    "websocket-stream": "^5.5.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
